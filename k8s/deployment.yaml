apiVersion: apps/v1
kind: Deployment
metadata:
  name: "smoketest"
  namespace: "agents-platform-eng"
  labels:
    app: "smoketest"
    team: "platform-eng"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "smoketest"
  template:
    metadata:
      labels:
        app: "smoketest"
        team: "platform-eng"
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: "sa-smoketest"
      containers:
        - name: "smoketest"
          image: "acragenticdemopoc.azurecr.io/agents/smoketest:latest"
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: "config-smoketest"
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
